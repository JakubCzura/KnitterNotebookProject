<UserControl x:Class="KnitterNotebook.Views.UserControls.SamplesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:KnitterNotebook.Views.UserControls"
             xmlns:viewModels="clr-namespace:KnitterNotebook.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="0" 
                  ItemsSource="{Binding Samples}" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectedItem="{Binding SelectedSample}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sample name" 
                                    Binding="{Binding Path=YarnName}" 
                                    Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Margin="10,10,10,10" Grid.Column="1">
            <TextBlock FontWeight="Bold" Margin="5,5,5,5" FontSize="20" Text="Yarn name"/>
            <TextBlock Margin="5,5,5,5" FontSize="20" Text="{Binding SelectedSample.YarnName}"/>
            <TextBlock FontWeight="Bold" Margin="5,5,5,5" FontSize="20" Text="Mashes x rows"/>
            <TextBlock Margin="5,5,5,5" FontSize="20" Text="{Binding SelectedSampleMashesXRows}"/>
            <TextBlock FontWeight="Bold" Margin="5,5,5,5" FontSize="20" Text="Needle size"/>
            <TextBlock Margin="5,5,5,5" FontSize="20" Text="{Binding SelectedSampleNeedleSize}"/>
        </StackPanel>
    </Grid>
</UserControl>